<div class="row">
	<div class="col s12 m12 l8 offset-l2">
		<h4>{{comments|length }}
			{{ (comments|length > 0) ? 'commentaires' : 'commentaire' }}
		</h4>
		{% for comment in comments %}
			<div>
				<p>
					<span class="flow-text" style="padding-right:10px;">
						{{comment.author.username}}
					</span>
					{{comment.createdAt|date("d M \\à\\ h:i")}}
				</p>
				<p>{{comment.content}}</p>
				<div class="divider"></div>
			</div>
		{% endfor %}
	</div>
</div>
